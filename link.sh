#!/bin/bash
# A very direct script for linking files into your Home directory.
# Add files as needed.

ln -sf gitconfig       $HOME/.gitconfig
ln -sf gitignore       $HOME/.gitignore
ln -sf todo.cfg        $HOME/.todo.cfg
ln -sf screenrc        $HOME/.screenrc
ln -sf bashrc          $HOME/.bashrc
ln -sf bash_profile    $HOME/.bash_profile
ln -sf rsync_exclude   $HOME/.rsync_exclude
#ln -sf zshrc           $HOME/.zshrc
